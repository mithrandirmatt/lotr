version: '3.9'

services:
	dev:
		build:
			context: ../..
			dockerfile: build/docker/Dockerfile
		image: lotr-dev:ubuntu-24.04
		volumes:
			- ../..:/workspace
		working_dir: /workspace
		environment:
			- DEBIAN_FRONTEND=noninteractive
		tty: true
		stdin_open: true
